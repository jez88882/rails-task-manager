<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
      <%= task.completed%>
      <%= link_to  "DELETE THIS", task_path(task), method: :delete, data: { confirm: "Sure anot?"} %>
    </li>
  <% end %>
</ul>
<%= link_to  "Add New Task", new_path %>
